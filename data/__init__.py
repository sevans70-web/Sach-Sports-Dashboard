"""Sach Sports Dashboard data package.

This file makes the repository's local ``data`` directory an explicit Python
package so imports such as ``from data.mlb_live import ...`` resolve to this
project instead of an unrelated installed package named ``data``.
"""
