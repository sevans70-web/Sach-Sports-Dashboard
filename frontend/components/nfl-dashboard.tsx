"use client";

import Link from "next/link";
import {useEffect,useMemo,useRef,useState,type ReactNode} from "react";
import {NFL_MARKETS,type NflMarketKey,type NflRankingRow} from "@/lib/nfl";

type NflPerformanceResponse={success:boolean;connected:boolean;writable?:boolean;hits:number;settled:number;pending:number;hitRate:number|null;total?:number;predictions?:any[]};
type ScheduleResponse={success:boolean;games:any[];qualifiedCount:number;filterMode?:string;updatedAt?:string;confirmedLineups?:number;pendingLineups?:number;totalLineups?:number;weekNumber?:number|null};
type RankingResponse={success:boolean;market?:NflMarketKey;rows:NflRankingRow[];updatedAt?:string;historySaved?:boolean;historyConnected?:boolean;historyWritable?:boolean};

const QB_MARKETS:NflMarketKey[]=["passing_yards","passing_tds","qb_rushing_yards","passing_rushing_yards"];
const OFFENSE_MARKETS:NflMarketKey[]=["rushing_yards","rushing_tds","receiving_yards","receptions","rushing_receiving_yards","anytime_td","first_td"];
const Q1_MARKETS:NflMarketKey[]=["q1_passing_yards","q1_receiving_yards","q1_rushing_yards"];

function useJson<T>(url:string,fallback:T,intervalMs=90_000){
  const cacheKey=`sach-nfl:${url}`;
  const[data,setData]=useState<T>(()=>{
    if(typeof window==="undefined")return fallback;
    try{return JSON.parse(sessionStorage.getItem(cacheKey)||"") as T}catch{return fallback}
  });
  const[loading,setLoading]=useState(true);
  useEffect(()=>{let active=true,seq=0;let controller:AbortController|null=null;setLoading(true);
    try{const cached=sessionStorage.getItem(cacheKey);if(cached)setData(JSON.parse(cached))}catch{}
    const run=()=>{seq+=1;const mine=seq;controller?.abort();controller=new AbortController();return fetch(url,{cache:"no-store",signal:controller.signal}).then(async r=>{if(!r.ok)throw new Error(`HTTP ${r.status}`);return r.json()}).then(v=>{
      // Never replace known-good UI with a failed/empty transport response.
      if(active&&mine===seq&&v?.success!==false){setData(v);try{sessionStorage.setItem(cacheKey,JSON.stringify(v))}catch{}}
    }).catch((e:any)=>{if(e?.name!=="AbortError")return}).finally(()=>{if(active&&mine===seq)setLoading(false)})};
    run();const id=setInterval(run,intervalMs);return()=>{active=false;clearInterval(id);controller?.abort()};
  },[url,intervalMs,cacheKey]);
  return{data,loading};
}

function marketMeta(key:NflMarketKey){return NFL_MARKETS.find(x=>x[0]===key)!}
function tabLabel(key:NflMarketKey){const m=marketMeta(key);return key.startsWith("q1_")?m[2]:`${m[1]} ${m[2]}`}
function headingLabel(key:NflMarketKey){return marketMeta(key)[2]}
function projectionText(row:NflRankingRow,market:NflMarketKey){
  if(market==="first_td")return row.modelProbability!=null?`${Number(row.modelProbability).toFixed(0)}% chance`:"Model unavailable";
  const v=row.modelProjection;if(v==null||!Number.isFinite(Number(v)))return "Insufficient history";const n=Number(v);
  if(["passing_yards","passing_rushing_yards","qb_rushing_yards","rushing_yards","receiving_yards","rushing_receiving_yards"].includes(market))return `${n.toFixed(1)} yds`;
  if(["q1_passing_yards","q1_receiving_yards","q1_rushing_yards"].includes(market))return `${n.toFixed(1)} Q1 yds`;
  if(["passing_tds","rushing_tds","anytime_td"].includes(market))return `${n.toFixed(1)} TD`;
  if(market==="receptions")return `${n.toFixed(1)} rec`; return n.toFixed(1);
}
function formatActual(value:number,market:NflMarketKey){const n=Number(value),shown=Number.isInteger(n)?String(n):n.toFixed(1);if(["passing_yards","passing_rushing_yards","qb_rushing_yards","rushing_yards","receiving_yards","rushing_receiving_yards"].includes(market))return `${shown} yards`;if(["q1_passing_yards","q1_receiving_yards","q1_rushing_yards"].includes(market))return `${shown} Q1 yards`;if(market==="receptions")return `${shown} reception${n===1?"":"s"}`;if(["passing_tds","rushing_tds","anytime_td","first_td"].includes(market))return `${shown} TD${n===1?"":"s"}`;return shown}
function formatMargin(value:number,market:NflMarketKey){return formatActual(value,market).replace(/s$/,"s")}
function gameDateTime(v?:string){
  if(!v)return "Game time TBD"; const d=new Date(v); if(Number.isNaN(d.getTime()))return "Game time TBD";
  return new Intl.DateTimeFormat("en-US",{timeZone:"America/Toronto",weekday:"short",month:"2-digit",day:"2-digit",hour:"numeric",minute:"2-digit",timeZoneName:"short"}).format(d);
}
function updatedLabel(v?:string){if(!v)return "Updated just now";const d=new Date(v);if(Number.isNaN(d.getTime()))return "Updated just now";return `Updated ${new Intl.DateTimeFormat("en-US",{timeZone:"America/Toronto",month:"short",day:"numeric",hour:"numeric",minute:"2-digit",timeZoneName:"short"}).format(d)}`}

function ScrollTabs({children,className="",showCue=true}:{children:ReactNode;className?:string;showCue?:boolean}){
  const ref=useRef<HTMLDivElement|null>(null);
  return <div className={`tabsWrap ${className} ${showCue?"":"noCue"}`}><div className="lineTabs" ref={ref}>{children}</div>{showCue?<button className="scrollCue" aria-label="Scroll more options" onClick={()=>ref.current?.scrollBy({left:220,behavior:"smooth"})}>›</button>:null}</div>;
}


function RankingCard({row,market}:{row:NflRankingRow;market:NflMarketKey}){
  const[open,setOpen]=useState(false);
  const projection=projectionText(row,market);
  const confidence=row.modelProbability!=null?`${Number(row.modelProbability).toFixed(0)}%`:"—";
  const liveProgress=row.liveCurrent!=null&&row.modelProjection!=null&&Number(row.modelProjection)>0
    ?Math.max(0,Math.min(100,Number(row.liveCurrent)/Number(row.modelProjection)*100))
    :null;
  return <article className="rankCard">
    <div className="rankNo">#{row.rank}<span className={`move ${row.movement||"same"}`}>{row.movement==="new"?"NEW":row.movement==="up"?`↑ ${Math.max(1,Number(row.previousRank||row.rank)-row.rank)}`:row.movement==="down"?`↓ ${Math.max(1,row.rank-Number(row.previousRank||row.rank))}`:"−"}</span></div>
    <div className="rankPhoto">{row.headshot?<img src={row.headshot} alt=""/>:<div>NFL</div>}</div>
    <div className="rankBody">
      <strong>{row.playerName}</strong>
      <span>{row.teamName}{row.matchup?` · ${row.matchup}`:""}</span>
      <span className="gameWhen">🗓 {gameDateTime(row.gameTime)}</span>
      {row.gameState==="in"?<div className="liveProgress"><b>● LIVE · {row.gameStatus||"In progress"}</b><span>Current: {row.liveCurrent==null?"Waiting for stat":formatActual(row.liveCurrent,market)}</span>{liveProgress!=null?<div className="progressTrack"><i style={{width:`${liveProgress}%`}}/></div>:null}</div>:null}
      <p className="projectionLine"><b>Sach Prediction:</b> {projection}</p>
      <p className="confidenceLine"><b>Confidence:</b> {confidence}</p>
      {row.resultStatus&&row.resultStatus!=="pending"?<div className="finalResult"><b>FINAL</b>{row.actualResult!=null?<span>Actual: {formatActual(row.actualResult,market)}</span>:<span>Final stat pending</span>}</div>:null}
    </div>
    <div className="rankGi"><span>GI</span><strong>{Number(row.giScore||0).toFixed(1)}</strong></div>
    <button className="intelButton" onClick={()=>setOpen(v=>!v)}>ⓘ {open?"Hide":"View"} Intelligence</button>
    {open?<div className="detail"><div className="detailMetric green"><span>CONFIDENCE</span><b>{row.modelProbability!=null?`${Number(row.modelProbability).toFixed(1)}%`:"—"}</b></div><div className="detailMetric"><span>REFERENCE LINE</span><b>{row.sportsbookLine??"—"}</b></div><div className="detailMetric gold"><span>BOOKS</span><b>{row.bookmakerCount||0}</b></div><div className="projectionBox"><span>SACH PREDICTION</span><strong>{projection}</strong></div><div className="why"><b>Why This Player Ranks Here</b><p>{row.summary}</p></div><Link className="fullCard" href={`/nfl/player/${encodeURIComponent(row.playerId)}?market=${encodeURIComponent(market)}&name=${encodeURIComponent(row.playerName)}&team=${encodeURIComponent(row.teamName)}&matchup=${encodeURIComponent(row.matchup)}&gi=${row.giScore}&prob=${row.modelProbability??""}&line=${row.sportsbookLine??""}&projection=${encodeURIComponent(String(row.modelProjection??""))}&img=${encodeURIComponent(row.headshot||"")}&position=${encodeURIComponent(row.position||"")}&teamId=${encodeURIComponent(row.teamId||"")}`}>Open full player card</Link></div>:null}
  </article>;
}

export default function NflDashboard(){
  const[group,setGroup]=useState<"QB"|"Offense"|"Q1">("QB"); const[market,setMarket]=useState<NflMarketKey>("passing_yards"); const[full,setFull]=useState(false); const[period,setPeriod]=useState("Today");
  const s=useJson<ScheduleResponse>("/api/nfl/schedule",{success:false,games:[],qualifiedCount:0},60_000); const r=useJson<RankingResponse>(`/api/nfl/rankings?market=${market}`,{success:false,rows:[]},180_000); const overallPerf=useJson<NflPerformanceResponse>(`/api/nfl/performance?period=${period}&group=${group}`,{success:false,connected:false,hits:0,settled:0,pending:0,hitRate:null},120_000); const marketPerf=useJson<NflPerformanceResponse>(`/api/nfl/performance?period=${period}&group=${group}&market=${market}`,{success:false,connected:false,hits:0,settled:0,pending:0,hitRate:null},120_000);
  const[movementRows,setMovementRows]=useState<NflRankingRow[]>([]);const[movementMarket,setMovementMarket]=useState<NflMarketKey|null>(null);const[dropped,setDropped]=useState<string[]>([]);const[showDropped,setShowDropped]=useState(false);const liveCacheRef=useRef<Record<string,Record<string,NflRankingRow>>>({});const rawRows=useMemo(()=>r.data.market===market?(r.data.rows||[]):[],[r.data,market]);
  useEffect(()=>{setMovementRows([]);setMovementMarket(null);setShowDropped(false);try{setDropped(JSON.parse(localStorage.getItem(`nfl-rank-dropped-${market}`)||"[]")||[])}catch{setDropped([])}},[market]);
  useEffect(()=>{if(!r.data.success||r.data.market!==market||!rawRows.length)return;const key=`nfl-rank-history-${market}`,eventKey=`nfl-rank-events-${market}`,dropKey=`nfl-rank-dropped-${market}`;let previous:Record<string,{rank:number;name:string}>={},events:Record<string,{movement:NflRankingRow["movement"];previousRank:number|null;rank:number}>={};try{previous=JSON.parse(localStorage.getItem(key)||"{}")||{};events=JSON.parse(localStorage.getItem(eventKey)||"{}")||{}}catch{}const current:Record<string,{rank:number;name:string}>={};const priorLive=liveCacheRef.current[market]||{};const next=rawRows.map(raw=>{const id=String(raw.playerId||raw.playerName),prior=priorLive[id];const keepLive=Boolean(prior&&prior.gameState==="in"&&raw.gameState!=="post"&&!(["hit","miss","push","void"] as string[]).includes(String(raw.resultStatus||"")));const row:NflRankingRow=keepLive?{...raw,gameState:raw.gameState==="in"?"in":prior.gameState,gameStatus:raw.gameStatus||prior.gameStatus,liveCurrent:raw.liveCurrent??prior.liveCurrent,liveProgressPct:raw.liveProgressPct??prior.liveProgressPct}:raw;current[id]={rank:row.rank,name:row.playerName};const old=previous[id],priorEvent=events[id];if(!old){events[id]={movement:"new",previousRank:null,rank:row.rank}}else if(row.rank!==old.rank){events[id]={movement:row.rank<old.rank?"up":"down",previousRank:old.rank,rank:row.rank}}else if(!priorEvent){events[id]={movement:"same",previousRank:old.rank,rank:row.rank}}return{...row,movement:events[id]?.movement||"same",previousRank:events[id]?.previousRank??null}});const newlyDropped=Object.entries(previous).filter(([id])=>!current[id]).map(([,v])=>v.name);let persistentDropped:string[]=[];try{persistentDropped=JSON.parse(localStorage.getItem(dropKey)||"[]")||[]}catch{}if(newlyDropped.length)persistentDropped=[...new Set([...newlyDropped,...persistentDropped])].slice(0,50);for(const id of Object.keys(current)){const name=current[id].name;persistentDropped=persistentDropped.filter(x=>x!==name)}liveCacheRef.current[market]=Object.fromEntries(next.map(row=>[String(row.playerId||row.playerName),row]));setDropped(persistentDropped);setMovementRows(next);setMovementMarket(market);try{localStorage.setItem(key,JSON.stringify(current));localStorage.setItem(eventKey,JSON.stringify(events));localStorage.setItem(dropKey,JSON.stringify(persistentDropped))}catch{}},[rawRows,market,r.data.market]);
  const rows=movementMarket===market?movementRows:rawRows; const live=s.data.games.filter((g:any)=>g.state==="in").length; const finals=s.data.games.filter((g:any)=>g.completed).length; const gameCount=s.data.games.length; const markets=group==="QB"?QB_MARKETS:group==="Q1"?Q1_MARKETS:OFFENSE_MARKETS;
  useEffect(()=>{if(!markets.includes(market))setMarket(markets[0]);setFull(false)},[group]); // eslint-disable-line react-hooks/exhaustive-deps
  useEffect(()=>setFull(false),[market]);
  const weekText=s.data.weekNumber?`Week ${s.data.weekNumber}`:"This Week";
  return <main className="nflShell">
    <section className="hero"><div className="heroTop"><Link href="/" className="nflMenu" aria-label="Open sports menu">▦⌄</Link><h1>NFL Intelligence Center</h1></div><p>Strongest NFL prop plays, matchup context and player intelligence in one place.</p></section>
    <div className="updated">{updatedLabel(s.data.updatedAt)}</div>
    <Link className="gamesEntry" href="/nfl/games"><b>🏈 {weekText.toUpperCase()} NFL GAMES</b><span>Schedule · Rosters · Intelligence ›</span></Link>
    <div className="snapshotHeading"><h2>{weekText} NFL Snapshot</h2><span>Always confirm starting lineups</span></div>
    <div className="snapshot"><article className="green"><span>GAMES</span><strong>{gameCount}</strong><small>{live} live · {finals} final</small></article><article><span>LINEUPS</span><strong>{s.data.confirmedLineups??0}/{s.data.totalLineups??gameCount*2}</strong><small>{s.data.pendingLineups??Math.max(0,gameCount*2-(s.data.confirmedLineups??0))} pending</small></article><article className="gold"><span>ALERTS</span><strong>0</strong><small>No active alerts</small></article></div>

    <section className="section performance"><h2 className="performanceTitle">📊 Prediction Performance</h2><details><summary>ⓘ How performance is measured</summary><div className="explain">Full-game predictions settle at final. First-quarter predictions settle when Q1 ends. Pending picks do not affect hit rate.</div></details>
      <ScrollTabs className="groupWrap" showCue={false}><button className={group==="QB"?"active":""} onClick={()=>setGroup("QB")}>🏈 QB</button><button className={group==="Offense"?"active":""} onClick={()=>setGroup("Offense")}>🏃 Offense</button><button className={group==="Q1"?"active":""} onClick={()=>setGroup("Q1")}>1Q</button></ScrollTabs>
      <h3>🌐 Overall NFL {group} Performance</h3><div className="periodTabs">{["Today","Yesterday","Week","Month","Season"].map(x=><button className={period===x?"active":""} onClick={()=>setPeriod(x)} key={x}>{x}</button>)}</div>
      <div className="overallMetrics"><article className="green"><span>Hit Rate</span><strong>{overallPerf.data.hitRate==null?"—":`${overallPerf.data.hitRate}%`}</strong></article><article><span>Correct / Settled</span><strong>{overallPerf.data.connected?`${overallPerf.data.hits} / ${overallPerf.data.settled}`:"—"}</strong></article><article className="gold"><span>Pending</span><strong>{overallPerf.data.connected?overallPerf.data.pending:"—"}</strong></article></div><p className="perfNote">Today&apos;s hit rate populates as saved predictions are graded.</p>
      <ScrollTabs>{markets.map(k=><button className={market===k?"active":""} onClick={()=>setMarket(k)} key={k}>{tabLabel(k)}</button>)}</ScrollTabs>
      <div className="perfGrid"><article className="green"><span>Hits / Predictions</span><strong>{marketPerf.data.connected?`${marketPerf.data.hits} / ${marketPerf.data.total||0}`:"—"}</strong></article><article><span>Pending</span><strong>{marketPerf.data.connected?marketPerf.data.pending:"—"}</strong></article><article className="gold"><span>Settled</span><strong>{marketPerf.data.connected?marketPerf.data.settled:"—"}</strong></article><article><span>Hit Rate</span><strong>{marketPerf.data.hitRate==null?"—":`${marketPerf.data.hitRate}%`}</strong></article></div><p className="perfNote">Results appear after games are graded.</p>{!marketPerf.data.connected||marketPerf.data.writable===false?<small className="historyWarning">Prediction history is temporarily unavailable. Live rankings can still load.</small>:null}
    </section>

    <section className="section rankings"><div className="rankHeader"><h2>Player Rankings</h2><p>Market-specific intelligence · live matchup context</p></div><ScrollTabs showCue={false}><button className={group==="QB"?"active":""} onClick={()=>setGroup("QB")}>🏈 QB</button><button className={group==="Offense"?"active":""} onClick={()=>setGroup("Offense")}>🏃 Offense</button><button className={group==="Q1"?"active":""} onClick={()=>setGroup("Q1")}>1Q</button></ScrollTabs><ScrollTabs>{markets.map(k=><button className={market===k?"active":""} onClick={()=>setMarket(k)} key={k}>{tabLabel(k)}</button>)}</ScrollTabs><div className="marketHead"><h2>{headingLabel(market)} Rankings</h2><p>GI Score blends recent performance, matchup context, market context and sample reliability.</p>{movementMarket===market&&dropped.length?<div className="droppedWrap"><button className="droppedButton" onClick={()=>setShowDropped(v=>!v)} aria-expanded={showDropped}>👁 Dropped from Top 25 ({dropped.length})</button>{showDropped?<div className="droppedPanel">{dropped.map(name=><span key={name}>{name}</span>)}</div>:null}</div>:null}</div><div className="cards">{(full?rows:rows.slice(0,5)).map(row=><RankingCard row={row} market={market} key={`${row.playerId}-${row.rank}`}/>)}</div>{!r.loading&&rows.length===0?<div className="empty">{market.startsWith("q1_")?`No verified ${headingLabel(market)} Q1 lines are currently posted.`:`No verified ${headingLabel(market)} lines are currently posted.`}</div>:null}{rows.length>5?<button className="viewFull" onClick={()=>setFull(v=>!v)}>{full?"Show Top 5 Only":"View Full Rankings"}</button>:null}</section>

    <style jsx global>{`
      .nflShell{max-width:780px;margin:0 auto;padding:8px 14px 72px;color:#fff}.hero{border:2px solid #d9b85d;border-radius:18px;padding:13px 15px;background:linear-gradient(110deg,rgba(217,184,93,.42),#0b0d0e 48%,rgba(0,78,47,.72))}.heroTop{display:flex;align-items:center;gap:12px}.nflMenu{display:grid;place-items:center;flex:0 0 44px;width:44px;height:44px;border:2px solid #20df7f;border-radius:13px;background:#0c0e0d;color:#fff!important;text-decoration:none!important;font-size:18px}.hero h1{font-size:28px;margin:0;font-weight:900}.hero p{margin:7px 0 0;color:#c8c8ce;line-height:1.3;font-size:15px}.updated{text-align:right;color:#9a9da4;margin:5px 2px 10px;font-size:12px;font-weight:700}.gamesEntry{display:flex;flex-direction:column;gap:2px;border:1.5px solid #d9b85d;border-left:7px solid #20df7f;border-radius:15px;padding:11px 14px;color:#fff!important;text-decoration:none!important;background:#0d0f10}.gamesEntry b{font-size:18px}.gamesEntry span{font-size:14px;color:#d4d5d8}.snapshotHeading{display:flex;align-items:end;justify-content:space-between;gap:10px;margin:11px 0 8px;flex-wrap:wrap}.snapshotHeading h2{font-size:23px;margin:0}.snapshotHeading span{color:#20df7f;font-size:11px;font-weight:800;text-align:left;flex:1 0 100%;white-space:nowrap}.snapshot{display:grid;grid-template-columns:repeat(3,1fr);gap:7px}.snapshot article,.overallMetrics article,.perfGrid article{border:1.5px solid #34373d;border-radius:15px;padding:8px;background:#111214;display:flex;flex-direction:column;gap:4px;min-width:0}.snapshot .green,.overallMetrics .green,.perfGrid .green{border-color:#20df7f}.snapshot .gold,.overallMetrics .gold,.perfGrid .gold{border-color:#d9b85d}.snapshot span,.overallMetrics span,.perfGrid span{color:#9da1a8;font-size:11px;white-space:nowrap}.snapshot strong,.overallMetrics strong,.perfGrid strong{font-size:22px}.snapshot small{color:#d0d1d4;font-size:11px}.section{margin-top:24px}.performanceTitle{font-size:27px!important;line-height:1.05!important;margin:0 0 9px!important}.section details{border:1.5px solid #34373d;border-radius:14px;padding:10px 12px;margin:9px 0 12px}.section summary{font-size:15px}.explain{color:#a9acb3;margin-top:8px;font-size:13px;line-height:1.35}.tabsWrap{position:relative;padding-right:30px;border-bottom:2px solid #34373d}.tabsWrap.noCue{padding-right:0}.lineTabs{display:flex;overflow-x:auto;scrollbar-width:none;border:0}.lineTabs::-webkit-scrollbar{display:none}.lineTabs button{flex:0 0 auto;white-space:nowrap;background:transparent;border:0;border-bottom:3px solid transparent;color:#fff;padding:10px 14px 8px;font-size:14px;font-weight:800}.lineTabs button.active{border-bottom-color:#f04f5f}.scrollCue{position:absolute;right:0;top:0;bottom:0;width:30px;border:1px solid #34373d;background:#0f1115;color:#d9b85d;font-size:25px;z-index:3}.periodTabs{display:grid;grid-template-columns:repeat(5,1fr)}.periodTabs button{min-width:0;background:#111319;border:1px solid #383b42;color:#fff;padding:10px 3px;font-size:12px;font-weight:700}.periodTabs button.active{background:#351015;border-color:#f04f5f}.performance h3{font-size:20px;margin:14px 0 7px}.overallMetrics{display:grid;grid-template-columns:repeat(3,1fr);gap:7px;margin:9px 0 5px}.perfGrid{display:grid;grid-template-columns:repeat(4,1fr);gap:6px;margin:9px 0 4px}.perfGrid article{padding:9px 7px}.perfGrid span{font-size:10px;white-space:normal;line-height:1.1}.perfGrid strong{font-size:19px}.perfNote{margin:4px 0 10px;color:#9da1a8;font-size:12px}.historyWarning{display:block;margin-top:7px;color:#9da1a8;font-weight:600;font-size:11px}.rankHeader{background:#0c0d0e;padding:12px 14px;margin-top:4px}.rankHeader h2{font-size:27px;margin:0 0 2px}.rankHeader p{margin:0;color:#a9acb3;font-size:14px;white-space:nowrap}.marketHead h2{font-size:24px;margin:16px 0 5px;white-space:nowrap}.marketHead p{font-size:14px;color:#a9acb3;line-height:1.35;margin:0}.rankCard{position:relative;display:grid;grid-template-columns:38px 78px 1fr 55px;gap:8px;border:3px solid #34373d;border-left:10px solid #20df7f;border-radius:20px;background:#111214;padding:11px 9px;margin:12px 0;overflow:hidden}.rankNo{font-size:21px;font-weight:900}.rankNo span{display:block;color:#9da1a8;margin-top:6px}.rankNo .move{font-size:10px}.rankNo .move.new{color:#d9b85d}.rankNo .move.up{color:#20df7f}.rankNo .move.down{color:#ff6b6b}.rankPhoto img,.rankPhoto>div{width:74px;height:74px;border-radius:50%;border:3px solid #d9b85d;object-fit:cover}.rankPhoto>div{display:grid;place-items:center;color:#d9b85d;font-weight:900}.rankBody strong{display:block;font-size:18px}.rankBody span{display:block;color:#a9acb3;font-size:13px;line-height:1.2;margin-top:3px}.rankBody b{display:block;font-size:14px;margin-top:7px}.rankBody p{color:#a9acb3;font-size:12px;line-height:1.25;margin:5px 0 0}.projectionLine{color:#fff!important;font-weight:800}.projectionLine b{display:inline!important;color:#d9b85d!important;font-size:inherit!important;margin:0!important}.confidenceLine{color:#fff!important}.confidenceLine b{display:inline!important;color:#20df7f!important;font-size:inherit!important;margin:0!important}.pickLine{color:#fff!important;font-weight:800}.pickLine b{display:inline!important;color:#20df7f!important;font-size:inherit!important;margin:0!important}.pickLine.under b{color:#d9b85d!important}.pickLine small{display:inline!important;color:#a9acb3!important;font-size:10px!important;margin:0!important}.rankGi{text-align:right}.rankGi span{display:block;color:#a9acb3;font-size:10px;font-weight:900}.rankGi strong{display:block;color:#d9b85d;font-size:19px;margin-top:2px}.intelButton{grid-column:2/-1;background:#080a09;color:#fff;border:2.5px solid #20df7f;border-radius:14px;padding:9px;font-size:15px;font-weight:700}.detail{grid-column:1/-1;display:grid;grid-template-columns:repeat(3,1fr);gap:6px}.detailMetric{border:1px solid #34373d;border-radius:10px;padding:8px;background:#0d0f10}.detailMetric.green{border-color:#20df7f}.detailMetric.gold{border-color:#d9b85d}.detailMetric span,.projectionBox span{display:block;color:#9da1a8;font-size:9px}.detailMetric b{display:block;margin-top:3px;font-size:13px}.projectionBox{grid-column:1/-1;border:1px solid #20df7f;border-radius:10px;padding:9px;background:#0d0f10}.projectionBox strong{display:block;color:#d9b85d;font-size:18px;margin-top:3px}.projectionBox small{display:block;color:#9da1a8;margin-top:3px;font-size:10px}.why{grid-column:1/-1;border-left:4px solid #20df7f;background:#151116;padding:10px}.why>b{color:#d9b85d;font-size:14px}.why p{color:#d7d8db;line-height:1.35;font-size:12px;margin-bottom:0}.fullCard{grid-column:1/-1;text-align:center;border:1.5px solid #34373d;border-radius:11px;padding:9px;color:#fff!important;text-decoration:none!important;background:#0d0f10;font-size:13px}.liveProgress{margin-top:7px;border:1px solid #20df7f;border-radius:10px;padding:7px;background:rgba(32,223,127,.06)}.liveProgress b{color:#20df7f!important;margin:0 0 3px!important;font-size:12px!important}.liveProgress span{color:#fff!important;font-size:12px!important;margin:0!important}.progressTrack{height:5px;background:#2b2e31;border-radius:99px;margin-top:5px;overflow:hidden}.progressTrack i{display:block;height:100%;background:#20df7f}.finalResult{margin-top:7px!important;display:flex;flex-direction:column;align-items:flex-start;gap:2px}.finalResult>b{display:inline-block!important;margin:0!important;padding:2px 7px;border:1.5px solid #d9b85d;border-radius:999px;color:#d9b85d!important;font-size:10px!important}.finalResult>span{font-size:13px;font-weight:900}.finalResult>small{font-size:10px;color:#c9cbd0}.finalResult.hit>span{color:#45ef8d}.finalResult.miss>span{color:#ff6b6b}.finalResult.push>span{color:#d9b85d}.gameWhen{color:#d9b85d!important;font-weight:800!important;font-size:11px!important}.droppedWrap{margin-top:8px}.droppedButton{border:1px solid #4b4f55;border-radius:10px;background:#0d0f10;color:#fff;padding:7px 10px;font-size:11px;font-weight:800}.droppedPanel{display:flex;flex-wrap:wrap;gap:5px;margin-top:7px}.droppedPanel span{border:1px solid #34373d;border-radius:999px;padding:4px 7px;background:#111214;color:#c9cbd0;font-size:10px}.empty{padding:20px;color:#a9acb3;text-align:center;font-size:13px}.viewFull{width:100%;background:#0d0f10;color:#fff;border:1.5px solid #34373d;border-radius:12px;padding:10px;font-size:14px}
      @media(max-width:430px){.hero h1{font-size:24px}.hero p{font-size:14px}.snapshotHeading h2{font-size:20px}.snapshotHeading span{font-size:10px;max-width:120px}.snapshot strong{font-size:19px}.rankHeader p{font-size:12px}.rankCard{grid-template-columns:32px 68px 1fr 48px;gap:6px}.rankPhoto img,.rankPhoto>div{width:64px;height:64px}.rankBody strong{font-size:16px}.rankBody span,.rankBody b{font-size:12px}.rankGi strong{font-size:17px}.detail{grid-template-columns:repeat(3,1fr)}.perfGrid span{font-size:9px}.perfGrid strong{font-size:17px}}
    `}</style>
  </main>;
}
