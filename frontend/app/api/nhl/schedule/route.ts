import {NextResponse} from "next/server";import {loadNhlOverview} from "@/lib/nhl";export const dynamic="force-dynamic";export async function GET(){try{const x=await loadNhlOverview();return NextResponse.json({success:true,games:x.games,updatedAt:x.updatedAt})}catch(e){return NextResponse.json({success:false,games:[],error:e instanceof Error?e.message:"NHL schedule unavailable"},{status:500})}}
