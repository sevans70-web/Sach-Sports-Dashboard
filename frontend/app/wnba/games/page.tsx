import Link from "next/link";
import {loadWnbaOverview} from "@/lib/wnba";
export const dynamic="force-dynamic";
function when(v:string|null){if(!v)return"Time TBD";return new Intl.DateTimeFormat("en-US",{timeZone:"America/Toronto",weekday:"short",month:"short",day:"numeric",hour:"numeric",minute:"2-digit"}).format(new Date(v))}
export default async function WnbaGames(){const data=await loadWnbaOverview();return <main style={{maxWidth:780,margin:"0 auto",padding:"12px 14px 70px",color:"#fff"}}><Link href="/wnba" style={{color:"#20df7f",fontWeight:800,textDecoration:"none"}}>‹ Back to WNBA</Link><h1 style={{marginBottom:4}}>Today’s WNBA Games</h1><p style={{color:"#a9acb3",marginTop:0}}>Schedule · Matchups · Live/final status</p><div style={{display:"grid",gap:10,marginTop:18}}>{data.games.map(g=><article key={g.gameId} style={{border:"1.5px solid #34373d",borderLeft:"7px solid #20df7f",borderRadius:15,padding:14,background:"#111214"}}><div style={{display:"flex",justifyContent:"space-between",gap:8,color:"#d9b85d",fontWeight:800,fontSize:12}}><span>{when(g.tipoff)}</span><span>{g.status}</span></div><h3 style={{margin:"12px 0 5px"}}>{g.awayTeam}{g.state!=="pre"&&g.awayScore!=null?` ${g.awayScore}`:""} @ {g.homeTeam}{g.state!=="pre"&&g.homeScore!=null?` ${g.homeScore}`:""}</h3></article>)}</div></main>}
